# Raw transcripts and generated review files are creator content.
# Keep them local/private unless intentionally sanitized.
raw/*
clean/*
extracted/*
reviewed/*
reviews/*

# Keep folder placeholders and example queue in git.
!raw/.gitkeep
!clean/.gitkeep
!extracted/.gitkeep
!reviewed/.gitkeep
!reviews/.gitkeep
!videos.json
